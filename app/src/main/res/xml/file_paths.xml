<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Where the "Take Photo" camera capture in the listing wizard writes its temp file to,
         before it's handed off as a content:// Uri (see ListingWizardScreen.kt's StepPhotos). -->
    <cache-path name="listing_photos" path="listing_photos/" />
</paths>
